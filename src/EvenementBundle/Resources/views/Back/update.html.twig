{% extends 'base.html.twig' %}

{% block contents %}

    <div class="content mt-3">
        &nbsp    <div class="card">
            <div class="card-header">
                <strong>créer categorie</strong>
            </div>
            <div class="card-body card-block">
                <form action="" method="post" class="form-horizontal">
                    <div class="row form-group">
                        &nbsp {{ form_start(form) }}
                        &nbsp   {{ form_widget(form) }}
                        {#                &nbsp   <input type="submit" class="btn btn-primary btn-sm " value="Create" />#}
                        <button type="submit" class="btn fa fa-edit" value="Edit" style="background-color: #FFFFFF;"> </button>
                        &nbsp{{ form_end(form) }}
                    </div>
                </form>
            </div>
        </div>

        <div>

            <ul>
                <a href="{{ path('afficherback') }}" class="btn btn-lg btn-info btn-block"><i class="fa fa-hand-o-left"> </i>Liste Evennement</a>

            </ul> </div>
    </div>

{% endblock %}
