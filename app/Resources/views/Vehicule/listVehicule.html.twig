{% extends 'layout.html.twig' %}
  {% block cnx %}

      {% if (user != null ) %}

          <a href="{{ path('fos_user_security_logout') }}"><i class="fa fa-user-circle"></i>LOGOUT</a>

          <a href="#"><i class="fa fa-envelope"></i>{{ user.Email }}</a>
      {% endif %}
      {% if (user == null)%}
          <a href="{{ path('fos_user_security_login') }}"><i class="fa fa-user-circle"></i>Sign In</a>
      {% endif %}
  {% endblock %}
{% block bg %}
    <section class="inner-banner">
        <div class="container">
            <ul class="thm-breadcrumb">
                <li><a href="index-2.html">TaxiCo</a></li>
            </ul><!-- /.thm-breadcrumb -->
            <h2>Vehicules</h2>
        </div><!-- /.container -->
    </section><!-- /.inner-banner -->
{% endblock %}
{% block contents %}

    <section id="pricing-area" class="section-padding overlay">
        <div class="container">
            <div class="row">
                <!-- Section Title Start -->
                <div class="col-lg-12">
                    <div class="section-title  text-center">
                        <h2>Catégories</h2>
                        <span class="title-line"><i class="fa fa-car"></i></span>
                    </div>
                </div>
                <!-- Section Title End -->
            </div>


            <div class="card  text-white">
                <img class="img-fluid" src="{{ asset('uploads/images/taxi.jpg') }}" alt="">

                <button class="btn btn-warning btn-sm"
                        id="center" style="color:white">
                    <a href="{{ path('vehicule_taxi') }}" class="btn btn-outline-secondary btn-lg pull-right">Consulter</a>
                </button>
                </div>
                    <br>

            <div class="card bg-dark text-white">
                <img class="img-fluid" src="{{ asset('uploads/images/covoiturage.jpg') }}" alt="">

                <button class="btn btn-warning btn-sm"
                        id="center" style="color:white">
                    <a href="{{ path('vehicule_covoiturage') }}" class="btn btn-outline-secondary btn-lg pull-right">Consulter</a>
                </button>

            </div>
            <p>  </p>
        </div>
    </section>

{% endblock %}
